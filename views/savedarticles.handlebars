<a id = "clearSavedArticlesBtn" class="nav-link" type="submit">Clear All Saved Articles</a>
 </form>
        </li>
      </ul>
    </div>
  </nav>


<div class="jumbotron">
    <div class="headersContainer">
        <h1 class="display-4">Favorite Holes</h1>
        <h3>Saved Articles</h3>
    </div>
</div>

{{#each savedArticles}}

    <div class="card py-0">
        <div class="card-header">
            <div class="row">
                <div class="col-md-10">
                    <h5 class="card-title"><a href={{link}}>{{title}}</a></h5>
                </div>
                <div class="col-md-2">
                        <a href="#" class="deleteBtn btn" data-id={{_id}}>DELETE</a>
                        <a href="#" class="noteBtn btn" data-id={{_id}} data-toggle="modal" data-target="#notesModal">NOTES</a>
                </div>
            </div>
        </div>
        <div class="card-body">
            <p class="card-text">{{summary}}</p>
        </div>
    </div>

    {{/each}}